apiVersion: v1
kind: Service
metadata:
  name: {{ include "enterprise_app.fullname" . }}
  labels:
    {{- include "enterprise_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enterprise_app.selectorLabels" . | nindent 4 }}
